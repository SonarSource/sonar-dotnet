<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.sonar-plugins.dotnet</groupId>
    <artifactId>parent</artifactId>
    <version>0.6</version>
  </parent>

  <groupId>org.codehaus.sonar-plugins.dotnet</groupId>
  <artifactId>sonar-dotnet</artifactId>
  <packaging>pom</packaging>
  <version>0.6</version>

  <name>Sonar .Net projects</name>
  <description>A Reactor Project for all the Sonar .Net code quality projects</description>

  <prerequisites>
    <maven>2.1</maven>
  </prerequisites>

  <properties>
    <sonar-build-version>2.1.1</sonar-build-version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.codehaus.sonar-plugins.dotnet</groupId>
        <artifactId>dotnet-commons</artifactId>
        <version>0.6</version>
        <type>jar</type>
      </dependency>
      <dependency>
        <groupId>org.codehaus.sonar-plugins.dotnet</groupId>
        <artifactId>sonar-plugin-dotnet-core</artifactId>
        <version>0.6</version>
        <type>sonar-plugin</type>
      </dependency>
      <dependency>
        <groupId>org.codehaus.sonar</groupId>
        <artifactId>sonar-plugin-api</artifactId>
        <version>${sonar-build-version}</version>
        <type>jar</type>
        <scope>compile</scope>
      </dependency>
      <dependency>
        <groupId>org.codehaus.sonar</groupId>
        <artifactId>sonar-squid</artifactId>
        <version>${sonar-build-version}</version>
      </dependency>      
    </dependencies>
  </dependencyManagement>

  <modules>
    <module>sonar-plugin-sourcemonitor</module>
    <module>sonar-plugin-dotnet-core</module>
    <module>sonar-plugin-fxcop</module>
    <module>sonar-plugin-coverage</module>
    <module>sonar-plugin-stylecop</module>
    <module>sonar-plugin-gallio</module>
    <module>sonar-plugin-dotnet-cpd</module>
    <module>sonar-plugin-gendarme</module>
  </modules>

  <profiles>
    <profile>
      <id>deploy-local</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.3</version>
            <executions>
              <execution>
                <phase>install</phase>
                <configuration>
                  <tasks>
                    <copy file="${project.build.directory}/${project.build.finalName}.jar" todir="${sonar-base-dir}/extensions/plugins" failonerror="false" />
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
