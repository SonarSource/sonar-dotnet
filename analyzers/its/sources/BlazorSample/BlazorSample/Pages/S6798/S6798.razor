@namespace BlazorSample.Pages.S6798

@functions {
    [JSInvokable] private void JSInvokablePrivateMethodInFunctionsBlock() { } // FN: Razor generation disabled in SL context, so in ITs too
}

@{
    [JSInvokable] void JSInvokableLocalFunctionInDirective() { }              // Compliant: local function
}
