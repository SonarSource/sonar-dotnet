Public Class Sample

    Public Sub Method(
                      a As String) ' Needs to be on a separate line to have a simple test scaffolding
        Dim b as String = a
        a = "value"
    End Sub

End Class
